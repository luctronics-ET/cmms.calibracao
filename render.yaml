services:
  - type: web
    name: cmms-calibracao
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    envVars:
      - key: SISCALIB_DB_URL
        sync: false   # valor definido no painel Render (não exposto no repo)
    healthCheckPath: /api/v1/health
